require('module-alias/register');
/*import {expect,use} from 'chai'
import chaiAsPromised from 'chai-as-promised';
use(chaiAsPromised);
import {describe,it} from 'mocha'


import {IOContainer,I, E}  from  '@ioc'
import {CORE_TYPE}         from '@core/types'
import InteractorExecutor from '@core/interactor/Executor';
import {SuccessViewModel,ErrorViewModel} from "@adapter/viewmodel";

let addressInteractor =IOContainer.get<I.IInteractor<E.ReadAddessRequest>>(CORE_TYPE.ReadAddressInteractor);


describe('USE CASE : Address Interactor',()=>{
    describe('Read Address from Address Interactor ',()=>{
        it('execute() should return an urban address viewmodel with type SuccessViewModel ', (done)=>{
            let request = { 
                id : '5c6f217412d1a4283ccd5e00'
            };
            addressInteractor.execute(request,(viewModel)=>{
                expect(viewModel).to.instanceOf(SuccessViewModel).and.have.property('httpStatusCode',200);
                done();
            })
            
        })
    });
    
})

*/