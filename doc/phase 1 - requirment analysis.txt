1. User can find their address either by searching desired address or by navigating cursor on google map.
2. Now the main challenge is that how can we will collect all addresses.possible solutions are bellow : 
        # Proposal 1        : Google map api(if available).
        # Proposal 2        : By user themself : It will works like a survey. we will share a link to anonymous user and they will give us feedback with their respective address.
                              Then we will summarize and filter all addresses.
        # Proposal 3        : Data Entry.

Tasks : 
    1. Please find the address standard of bangladesh.
    2. Required attributes of address.
    2. Please analize which data storage will be better. sql or no sql and why?.
